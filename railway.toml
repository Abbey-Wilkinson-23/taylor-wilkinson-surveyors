[build]
buildCommand = "pip install -r requirements.txt"

[deploy]
startCommand = "python -m alembic upgrade head && python seed_postcode.py && uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
