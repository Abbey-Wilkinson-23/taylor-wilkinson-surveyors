[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["python -m alembic upgrade head && python seed_postcode.py"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
